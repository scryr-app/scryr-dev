[project]
name = "scryr-tests"
version = "0.2.0"
description = "Test workspace for Scryr manifest and samples"
requires-python = "==3.14.6"
dependencies = ["pytest>=8.0.0", "scryr", "scryr-samples"]

[tool.uv]
package = false

[tool.uv.sources]
scryr = { workspace = true }
scryr-samples = { workspace = true }
