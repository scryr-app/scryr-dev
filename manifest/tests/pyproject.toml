[project]
name = "scryr-tests"
version = "0.5.0"
description = "Test workspace for Scryr manifest and samples"
requires-python = "==3.14.6"
dependencies = ["pytest>=8.0.0", "scryr"]

[tool.uv]
package = false

[tool.uv.sources]
scryr = { workspace = true }
