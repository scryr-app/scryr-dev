<!-- Header owns the shared page drawer on both landing and documentation pages. -->
<div class="page">
  <header class="header"><slot name="header" /></header>
  <div class="main-frame"><slot /></div>
</div>

<style>
  .page { display: flex; flex-direction: column; min-height: 100vh; }
  .header { z-index: var(--sl-z-index-navbar); position: fixed; inset-inline-start: 0; inset-block-start: 0; width: 100%; height: var(--sl-nav-height); padding: var(--sl-nav-pad-y) var(--sl-nav-pad-x); border-bottom: 1px solid var(--sl-color-hairline-shade); background: var(--sl-color-bg-nav); }
  .main-frame { --sl-content-inline-start: 0rem; padding-top: calc(var(--sl-nav-height) + var(--sl-mobile-toc-height)); }
  @media print {
    .header { display: none; }
    .main-frame { padding: 0; }
  }
</style>
